# Voice profile: chat prompt

<!-- DELETE every HTML comment in this file once you have filled it in. Unlike
     the other files this one is pasted somewhere by hand, so leftover
     scaffolding goes out with it.

     Optional, and NEVER loaded during drafting. This is a condensed,
     self-contained rendering of the whole profile for pasting where a
     directory cannot be read: another tool's custom-instructions field, a
     system prompt, a colleague's setup.
     It is DERIVED. Write it from the finished core.md, registers and
     archetypes, and regenerate it whenever those change. A rule that exists
     only here is a rule no draft will ever load.
     Keep it short enough to paste into a field with a character limit, which
     usually means core plus the one or two registers and archetypes that
     matter, at lower fidelity and without citations.
     See ../contract.md for what it is for, and ../bootstrap.md, step 5, for
     when to render it. -->

## Who is writing

<!-- One or two sentences: what this person writes, to whom, in which
     languages. Enough for a tool with no other context. -->

## Always

<!-- The core rules, compressed to the ones that change a draft most. Rhythm,
     structure, stance, the speech acts. Drop the citations here: this is the
     pasteable rendering, and the evidence lives in core.md. -->

## Never

<!-- The anti-patterns, as flat bans. Only the ones a rate comparison
     supported; a suspicion that did not earn a place in core.md does not
     earn one here. -->

## By language

<!-- One short block per register, holding only what changes with the
     language. -->

## By kind of message

<!-- One short block per archetype worth carrying. Where this rendering has to
     be short, keep the archetypes whose voice differs most from core and drop
     the ones that barely move. -->

## If unsure

<!-- What to do when the situation matches nothing above: which default to
     fall back on, and whether to ask. -->
