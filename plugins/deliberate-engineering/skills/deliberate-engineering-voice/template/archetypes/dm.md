# Archetype: dm

<!-- DELETE every HTML comment in this file once you have filled it in. They are
     scaffolding, not profile content: left in place they load alongside your
     rules whenever this archetype matches and compete with them.

     Loaded when the communication is a one-to-one direct message.
     Archetype names are communication TYPES, never tools: `dm`, not the name
     of the chat app you happen to use. One tool carries several types, and a
     DM and a channel post do not sound alike.
     See ../../contract.md for the suggested starting set of nine, and for the
     rule that no match is a supported state (core plus register only). A fresh
     copy of this template ships one archetype, so expect "no archetype matched"
     on most drafts at first: each declaration names the one to write next.
     Cite every rule: [n/N; ids], [interview], [provisional: n/N].
     See ../../contract.md, "Citations". -->

## When this applies

<!-- The artifacts this archetype covers, and the nearby ones it does not. -->

## Length and shape

<!-- Typical length. Whether you split into several messages. How much context
     you set up front, given that the reader is one known person.
     Cite each: [n/N; ids]. -->

## Opening and closing

<!-- Whether you greet at all, and what ends the message.
     Cite each: [n/N; ids]. -->

## Shift from core

<!-- Only what MOVES relative to core: formality, directness, punctuation,
     emoji. Rules core already covers are not repeated here.
     Cite the shift itself, both sides where you have them:
     [this type n/N; core n/N]. -->

## Not in this archetype

<!-- What this type forbids that core allows: structures, register, length.
     Same evidence bar as any other ban: [corpus n/N; contrast n/N]. -->

## Examples

<!-- Two or three verbatim messages of this type. Outside the budget.
     Cite the sample each one came from: [id]. -->
